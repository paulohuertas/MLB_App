@model MLB_App.Models.PlayerDetail

@{
    ViewBag.Title = "PlayerDetail";
}

<h2>Player Detail</h2>

<div>
    <h4>Player Details</h4>
    <hr />
    <dl class="dl-horizontal">
    </dl>
</div>
<table class="table table-hover">
    <tr>
        <th scope="col" class="text-center">Player</th>
        <th scope="col" class="text-center">Jersey Num</th>
        <th scope="col" class="text-center">Team</th>
        <th scope="col" class="text-center">Birthday</th>
        <th scope="col" class="text-center">Position</th>
        <th scope="col" class="text-center">Last Game Played</th>
    </tr>
    <tr class="active">
        <td scope="row" class="text-center"><figure><img src="@Model.body.mlbHeadshot" style="width: 150px; height: 100px;" /><figcaption>@Model.body.longName</figcaption></figure></td>
        <td scope="row" class="text-center col-md-2">@Model.body.jerseyNum</td>
        <td scope="row" class="text-center col-md-2">@Model.body.teamAbv</td>
        <td scope="row" class="text-center col-md-2">@Model.body.bDay</td>
        <td scope="row" class="text-center col-md-2">@Model.body.pos</td>
        <td scope="row" class="text-center col-md-2">@Model.body.lastGamePlayed.Substring(9)</td>
    </tr>
</table>
    <p>
        @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
        @Html.ActionLink("Back to List", "Index")
    </p>
